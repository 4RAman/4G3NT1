# Development/test dependencies (Windows or any desktop).
-r requirements.txt
pytest>=8.0
pytest-asyncio>=0.23
bleak>=0.22  # tools/ble_probe.py; becomes a runtime dep in Phase 3

# ESP32 toolchain (firmware/README.md). The MicroPython stubs are not here on
# purpose - they install into typings/ with --target, not into the venv.
esptool>=5.0
mpremote>=1.24
