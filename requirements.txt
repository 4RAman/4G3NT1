# Runtime dependencies for the Pi.
# Prefer creating the venv with --system-site-packages and installing
# python3-gpiozero + python3-lgpio via apt (see SETUP.md); pip versions
# below work too (piwheels provides ARM wheels).
gpiozero>=2.0
lgpio; sys_platform == "linux"
httpx>=0.27
ollama>=0.4
bluez-peripheral; sys_platform == "linux"
fastapi>=0.110
uvicorn>=0.29
